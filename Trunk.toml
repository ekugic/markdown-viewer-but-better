[[hooks]]
stage = "pre_build"
command = "echo"
command_arguments = ["Building Markdown Viewer..."]

[build]
target = "index.html"
dist = "dist"
public_url = "/"

[watch]
ignore = ["dist", "src-tauri"]

[serve]
address = "127.0.0.1"
port = 8080
